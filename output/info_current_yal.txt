header:


EXPRESIONES ENCONTRADAS:
let delim = [' ''\t''\n']
let ws = delim+
let letter = ['A'-'C''a'-'c']
let digit = ['0'-'2']
let id = letter(letter|digit)*

REGLAS ENCONTRADAS:
ws
| id        { return ID }
| '+'       { return PLUS }
| '*'       { return TIMES }
| '('       { return LPAREN }
| ')'       { return RPAREN }

trailer:

